## Installation

```sh
$ git clone https://github.com/itsjesusurias/dotfiles.git ~/dotfiles
$ cd ~/dotfiles
$ chmod -R +x **/*.sh
$ chmod -R +x *.sh
$ chmod +x setup.sh
$ chmod +x setup-new-machine.sh
$ ./setup-new-machine.sh
```
